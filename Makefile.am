# If you want to build these components, please cd to their respective
# directories and type `make'.
AUTOMAKE_OPTIONS = foreign

subdirs = mtxmatheval src include Interrogator Gui LookupTables RealtimeMaps RuntimeSliders 

SUBDIRS = $(subdirs) 

DIST_SUBDIRS = $(subdirs)

DEPDIR = ${subdirs}/.deps

CLEANFILES = *~ core -rf autom4te.cache

EXTRA_DIST = configure CREDITS VERSION AUTHORS TODO README INSTALL scripts/cross-link

