# If you want to build these components, please cd to their respective
# directories and type `make'.
AUTOMAKE_OPTIONS = foreign


subdirs = src include

SUBDIRS = $(subdirs) 

DIST_SUBDIRS = $(subdirs)

DEPDIR = ${subdirs}/.deps

CLEANFILES = *~ core -rf .dep*

EXTRA_DIST = configure CREDITS VERSION AUTHORS TODO README INSTALL

