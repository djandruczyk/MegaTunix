#!/bin/sh

. cross64
CPUS=`cat /proc/cpuinfo |grep -c ^processor`

make distclean
cross64-configure ./configure --prefix=/opt/crossroot64/dest $* && \
	make -j${CPUS} && rm -rf /opt/crossroot64/dest/* && make install && \
	./contrib/builder64 && \
. uncross 
# make distclean
